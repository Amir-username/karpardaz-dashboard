import LoginForm from "@/components/app/LoginForm/LoginForm";

function LoginPage() {
  return (
    <main className="flex w-full h-full py-32 items-center justify-center">
      <LoginForm />
    </main>
  );
}

export default LoginPage;
